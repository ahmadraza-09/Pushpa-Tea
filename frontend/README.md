# Pushpa Tea - fixed project

## Run
```bash
npm install
npm run dev
```

## Main fixes
- Restored missing data exports used by pages/components.
- Added `whatsappLink` helper.
- Fixed `CartContext.remove`.
- Wrapped the app in `CartProvider`.
- Added missing `Terms` page and import.
- Added missing `NotFound` import in ProductDetail.
- Restored social icon imports in Footer.
- Replaced malformed Markdown-style image URLs with real URLs.
- Restored `homeStats`.
- Included ScrollToTop in the router.
